@startuml
skinparam conditionStyle inside
|#E8F2FC|用户交互界面|
|#FCE8E8|主系统应用核心|
|#E8FCE8|Blender渲染与底层引擎|

|用户交互界面|
start
:验证身份并登录系统;
:展示相匹配的角色工作台;

repeat
  :选择并输入多模态设计需求;

  |主系统应用核心|
  switch (识别输入数据类型)
  case ( 自然语言文本 )
    :调用大模型执行语义意图解析;
  case ( 二维规划草图 )
    :调用图像引擎进行图面特征提取;
  case ( 空间精确坐标 )
    :校验并格式化处理空间参数点集;
  case ( 预设风格模板 )
    :匹配关联的底层资产与环境变量;
  endswitch
  :统一封装为跨进程执行脚本与参数矩阵;

  |Blender渲染与底层引擎|
  :装载所需的三维模型与材质资产;
  :执行场景数据更新与节点计算;
  :输出三维实时渲染流反馈;

  |用户交互界面|
backward: 重新修正参数或补充自然语言需求;
repeat while (判定是否满意当前生成效果？) is (不满意)

-> 满意并确认;
:保存工程档案与执行结果;
stop
@enduml